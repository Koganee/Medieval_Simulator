// Define a global object to hold shared variables
const GlobalState = {
    propagandaTarget: null,
    trustInKing: 200,
};

// Export the object to make it accessible globally
window.GlobalState = GlobalState;
